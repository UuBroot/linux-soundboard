#!/bin/sh
SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"
export PYTHONPATH="@PYTHONDIR@:$PYTHONPATH"

# For AppImage, use the bundled Python if available
if [ -n "$APPDIR" ]; then
    PYTHON="$APPDIR/usr/bin/python3"
else
    PYTHON="@PYTHON@"
fi

exec "$PYTHON" "@PYTHONDIR@/main.py" "$@"